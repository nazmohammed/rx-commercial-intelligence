# ── Azure Bot Service (legacy — only used by src/bot/, kept until cleanup) ──
BOT_APP_ID=
BOT_APP_PASSWORD=

# ── Azure AI Foundry ──
FOUNDRY_PROJECT_ENDPOINT=
FOUNDRY_QUERY_ENGINE_AGENT_ID=
FOUNDRY_ANALYST_AGENT_ID=

# ── Power BI (auth via DefaultAzureCredential / managed identity) ──
# Local dev: run `az login` — DefaultAzureCredential uses your CLI token.
# Production: Container App managed identity.
PBI_WORKSPACE_ID=4435d932-4c62-46fd-ba3f-dd41a0d6d2f4
PBI_DATASET_ID=192c798b-0a94-4791-8520-0922452167aa

# ── Local dev only (optional) ──
# UPN to impersonate for PBI Row-Level Security when testing locally.
TEST_USER_UPN=

# UPN the FastAPI app should use when no Easy Auth header is present
# (i.e. running uvicorn directly without Container Apps in front).
LOCAL_DEV_UPN=

# Frontend origin allowed by FastAPI CORS during local dev.
FRONTEND_ORIGIN=http://localhost:5173

# ── App ──
PORT=3978
LOG_LEVEL=INFO
